# Dependencies
node_modules

# Build output
dist
coverage

# Retained app bundles from `cognite apps deploy` (signed before publish)
.cognite-bundles

# Local HTTPS certificates from `cognite apps setup-https`
certificates

# Environment variables
.env
.env.local

# Editor directories
.vscode
.idea

# OS files
.DS_Store
Thumbs.db

# Certification-guide working notes: includes Cognite's own certification
# PDF (their copyrighted training material, not this app's content) plus
# scratch planning docs. Their distilled substance already lives in
# SPEC.md / App-Brief.md at the repo root.
/info

